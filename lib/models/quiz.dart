class Quiz {
  String name;
  String id;
  String grade;
  List<String> questions;

  Quiz(
      {required this.id,
      required this.name,
      required this.grade,
      required this.questions});
}
